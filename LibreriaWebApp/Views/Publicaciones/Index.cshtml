@using LogicaNegocio;
@model List<Publicacion>;

<h1>Publicaciones</h1>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Nombre</th>
            <th scope="col">Estado</th>
            <th scope="col">Precio</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (Publicacion unaPublicacion in Model)
        {
            <tr>
                <th scope="row">@unaPublicacion.Nombre</th>
                <td>@unaPublicacion.Estado</td>
                <td>$ @unaPublicacion.CalcularPrecio()</td>
                @if (unaPublicacion.Estado == EstadoPublicacion.Abierta)
                {
                    if (unaPublicacion is Venta)
                    {
                        <td><a class="btn btn-primary" href="~/Publicacion/Comprar">Comprar</a></td>
                    }
                    else
                    {
                        <td><a class="btn btn-primary" href="~/Publicacion/OfertarSubasta">Ofertar en subasta</a></td>
                    }
                }
                else
                {
                    <td></td>
                }
            </tr>
        }
    </tbody>
</table>